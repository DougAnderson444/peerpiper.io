import { writable } from 'svelte/store'

export const hypnsNode = writable(0)
export const ipfsNode = writable(0)
export const myInstance = writable(0)
export const appSection = writable(0)
export const subdomain = writable(0)
